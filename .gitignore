# Secrets — never commit your API key
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.pyc
.python-version

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Lab raw outputs (models generated code/answers) — never commit; only distilled results.json
harness/out/
# Author grading reference — keep local to avoid contaminating re-runs
harness/suites/**/ANSWER-KEYS.md

# Android release signing material — NEVER commit (defense in depth)
**/android/key.properties
**/android/app/*.jks
**/android/app/*.keystore

# Lab-local app builds live in their own repo, not in the hub (hub keeps distilled results)
opus48-vs-gpt56sol-mobile-aab/filter_brew_companion/
